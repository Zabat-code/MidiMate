[
  ["// src/main.js - Punto de entrada", "// src/main.js - Entry point"],
  ["// Configuración", "// Configuration"],
  ["// Idiomas", "// Languages"],
  ["// Controls - Keyboard (PC y MIDI)", "// Controls - Keyboard (PC and MIDI)"],
  ["// Controls - Eventos globales", "// Controls - Global events"],
  ["// Onboarding (tutorial de primer uso)", "// Onboarding (first-use tutorial)"],
  ["// Exportar video", "// Export video"],
  ["// 0. MANEJO GLOBAL DE ERRORES (lo primero de todo, antes de tocar el DOM)", "// 0. GLOBAL ERROR HANDLING (first of all, before touching the DOM)"],
  ["// 1. EXPONER GLOBALES", "// 1. EXPOSE GLOBALS"],
  ["// 2. INICIALIZACIÓN", "// 2. INITIALIZATION"],
  ["  // Cada bloque va en su propio try/catch: si uno falla, el resto de la app\r\n  // sigue arrancando en vez de quedar todo en blanco (antes, un solo error\r\n  // en cualquier punto de init() cortaba TODO lo que venía después).", "  // Each block runs in its own try/catch: if one fails, the rest of the app\r\n  // keeps starting up instead of going blank (before, a single error at any\r\n  // point in init() would cut off EVERYTHING that came after)."],
  ["    console.error(`[PianoApp] Falló el paso de inicio \"${name}\":`, err);", "    console.error(`[PianoApp] Startup step \"${name}\" failed:`, err);"],
  ["      try { showToast(`⚠️ Error al iniciar (${name}). Revisa el reporte en Ajustes.`); } catch (e) {}", "      try { showToast(`⚠️ Error starting up (${name}). Check the report in Settings.`); } catch (e) {}"],
  ["    // Crear el AudioContext de forma perezosa en el primer gesto del usuario\r\n    // (clic/tecla), que es cuando los navegadores permiten arrancar audio.", "    // Create the AudioContext lazily on the first user gesture\r\n    // (click/key), which is when browsers allow audio to start."],
  ["        // Tras el primer gesto los nodos del grafo ya son reales: los exponemos\r\n        // a window para depuración (antes eran undefined porque el grafo es lazy).", "        // After the first gesture the graph nodes are real: we expose them\r\n        // on window for debugging (before they were undefined because the graph is lazy)."],
  ["      } catch (e) { console.warn('No se pudo iniciar el audio:', e); }", "      } catch (e) { console.warn('Could not start audio:', e); }"],
  ["  // Estos dos son críticos para que se vea algo en pantalla: van fuera del\r\n  // patrón safeStep individual pero protegidos igual, para asegurarnos de\r\n  // que SIEMPRE se intenten aunque algo anterior haya fallado.", "  // These two are critical for something to appear on screen: they sit outside\r\n  // the individual safeStep pattern but are equally protected, to make sure\r\n  // they ALWAYS run even if something earlier failed."],
  ["console.log('🎹 Iniciando Piano App...');", "console.log('🎹 Starting Piano App...');"],
  ["'eventos'", "'events'"],
  ["'paneles'", "'panels'"],
  ["'flags-y-fondo'", "'flags-and-background'"],
  ["'layout-inicial'", "'initial-layout'"],
  ["'render-inicial'", "'initial-render'"]
]
